# Distill Webhook Visualizer - Environment Variables
# Copy this file to .env for local development
# In Coolify, set these in the Environment Variables section

# Application Configuration
DOMAIN=localhost
PORT=9988
HOST=0.0.0.0
LOG_LEVEL=info

# User Authentication
# IMPORTANT: Change these passwords in production!
RAMU_PASSWORD=changeme
LIGIGY_PASSWORD=changeme
QUASI_PASSWORD=changeme

# CORS Configuration
# For production, set to your domain(s)
# Example: CORS_ORIGINS=https://distill.example.com,https://api.example.com
CORS_ORIGINS=http://localhost:9988,http://localhost:3000

# Database (SQLite - no configuration needed)
DATABASE_URL=sqlite:///./data/monitoring.db
